import React from 'react';
import PagePlaceholder from './PagePlaceholder';

const Settings = () => (
  <PagePlaceholder
    title="Settings"
    description="Configure workspace preferences, roles, and integrations."
  />
);

export default Settings;

