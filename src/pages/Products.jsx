import React from 'react';
import PagePlaceholder from './PagePlaceholder';

const Products = () => (
  <PagePlaceholder
    title="Products"
    description="Track inventory, pricing, and catalog updates across channels."
  />
);

export default Products;

